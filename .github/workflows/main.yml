name: IRPX Config Grabber Automation

on:
  schedule:
    - cron: '0 * * * *' # اجرا در دقیقه 0 هر ساعت
  workflow_dispatch: # امکان اجرای دستی از تب Actions

permissions:
  contents: write # اجازه ثبت تغییرات در مخزن

jobs:
  update-configs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install requests

      - name: Clean Old Files
        run: |
          mkdir -p export
          rm -f export/*.txt
          
      - name: Run Config Grabber
        run: python main.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@users.noreply.github.com"
          git add export/*.txt count.txt
          # چک کردن برای تغییرات قبل از کامیت
          git diff --quiet && git diff --staged --quiet || git commit -m "🚀 Auto-update configs: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          git push
